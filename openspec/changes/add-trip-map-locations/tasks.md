# Tasks

- [x] Add nullable `pickupLocation` and `dropoffLocation` to Prisma Trip model and SQL migration.
- [x] Return and persist the fields in `app/api/trips/route.ts`.
- [x] Return and persist the fields in `app/api/trips/[id]/route.ts`.
- [x] Add fields to `components/trip-form.tsx` create/edit state and submit payload.
- [x] Add fields to `components/schedule-list.tsx` edit sheet, payload, and list display.
- [x] Run Prisma generate and TypeScript check.
- [ ] Resolve existing repository lint errors before lint can pass globally.
